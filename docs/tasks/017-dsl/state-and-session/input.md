All agents created from a single DSL file should share the same state, so they can exchange data.

We can also use ADK agent state to maintain the context variables we currently maintain via ctx.vars across the implementation.

We need to figure out how session is managed.